name: flutter_blue_plus_darwin
version: 9.0.3
description: Stub - no NativeCallable
environment:
  sdk: '>=3.12.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus_platform_interface: ^9.0.0
flutter:
  plugin:
    platforms:
      ios:
        pluginClass: FlutterBluePlusPlugin
      macos:
        pluginClass: FlutterBluePlusPlugin

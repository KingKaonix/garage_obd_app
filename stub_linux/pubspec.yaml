name: flutter_blue_plus_linux
version: 9.0.3
description: Stub replacement to avoid bluez/dbus FFI crash
environment:
  sdk: '>=3.12.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus_platform_interface: ^9.0.0
flutter:
  plugin:
    platforms:
      linux:
        pluginClass: FlutterBluePlusLinuxPlugin
